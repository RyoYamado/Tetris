# Тетрис - Полная система таблицы лидеров

## Что было добавлено

### 1. Новые файлы:

- **`js/leaderboard.js`** - Главный модуль управления лидеров
  - LeaderboardManager класс с полным функционалом
  - Методы для получения топ игроков, поиска, рейтингов
  - Система кэширования для оптимизации
  
- **`leaderboard.html`** - Страница таблицы лидеров
  - Отображение топ 10 / топ 100 / рядом со мной
  - Поиск по игрокам
  - Статистика сервера
  - Адаптивный дизайн
  
- **`js/leaderboard-page.js`** - Контроллер страницы
  - Управление фильтрами
  - Обработка поиска
  - Загрузка и отображение данных
  
- **`css/leaderboard.css`** - Полная стилизация
  - Современный дизайн с темой приложения
  - Адаптивный к всем размерам экранов
  - Анимации и переходы

- **`LEADERBOARD_SYSTEM.md`** - Полная документация системы

### 2. Обновленные файлы:

- **`js/firebase.js`** - Добавлены экспорты `where` для расширенных запросов
- **`game.html`** - Добавлена кнопка "Таблица лидеров" в навигацию
- **`js/game-page.js`** - Обработчик клика на кнопку таблицы лидеров
- **`js/game-manager.js`** - Упрощен, удалены методы отображения (теперь на отдельной странице)
- **`js/auth.js`** - Удалены вызовы updateLeaderboard (больше не нужны в AuthManager)

## Возможности системы

✅ **Таблица лидеров топ 10 игроков**
- Сортировка по лучшему результату
- Отображение позиции, имени, очков, количества игр, среднего результата

✅ **Поиск игроков**
- Real-time поиск по имени
- Частичное совпадение (регистронезависимый)

✅ **Фильтры:**
- Топ 10 лучших
- Рядом со мной (±5 позиций)
- Все игроки

✅ **Статистика:**
- Личный рейтинг и лучший результат
- Средний результат на игру
- Общая статистика сервера (всего игроков, игр, максимум, среднее)

✅ **Автоматическое обновление рейтинга**
- При завершении игры автоматически сохраняется результат
- Обновляется позиция в рейтинге

✅ **Кэширование**
- Top Players: 30 сек кэш
- User Stats: 10 сек кэш
- Кнопка "Обновить" для принудительного обновления

✅ **Безопасность**
- XSS защита (экранирование HTML)
- Требуется авторизация для доступа
- Данные защищены Firebase Security Rules

✅ **Адаптивность**
- Desktop: полная таблица с 5 колонками
- Tablet: 3 колонки
- Mobile: оптимизировано для маленьких экранов

## Как использовать

### 1. Запуск приложения

1. Откройте `index.html` в браузере
2. Зарегистрируйтесь или авторизуйтесь
3. Нажмите "Начать" для игры

### 2. Доступ к таблице лидеров

- Из игровой страницы нажмите кнопку "Таблица лидеров" в навигации
- Или откройте `leaderboard.html` напрямую (если вы авторизованы)

### 3. Навигация в таблице лидеров

**Фильтры:**
- Нажмите "Топ 10" чтобы увидеть 10 лучших
- Нажмите "Около меня" чтобы увидеть игроков рядом с вами
- Нажмите "Все игроки" чтобы увидеть весь список

**Поиск:**
- Введите имя игрока в строку поиска
- Нажмите "Поиск" или Enter

**Обновление:**
- Нажмите кнопку "🔄 Обновить" для принудительного обновления данных

## Firebase правила безопасности

Рекомендуемые правила для коллекции `users`:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Пользователь может читать свои данные
      allow read: if request.auth.uid == userId;
      // Пользователь может читать все данные для таблицы лидеров
      allow read: if request.auth != null;
      // Пользователь может записывать только свои данные
      allow write: if request.auth.uid == userId;
    }
  }
}
```

## API LeaderboardManager

### Инициализация

```javascript
const leaderboardManager = new LeaderboardManager(authManager);
```

### Методы

```javascript
// Получить топ N игроков
const topPlayers = await leaderboardManager.getTopPlayers(10);

// Получить рейтинг конкретного игрока
const userStats = await leaderboardManager.getUserStats(userId);

// Получить игроков рядом с указанным
const nearbyPlayers = await leaderboardManager.getNearbyPlayers(userId);

// Поиск по имени
const results = await leaderboardManager.searchPlayers("John", 20);

// Получить профиль игрока
const profile = await leaderboardManager.getPlayerProfile(userId);

// Получить общую статистику
const stats = await leaderboardManager.getLeaderboardStats();

// Очистить кэш
leaderboardManager.clearCache();

// Принудительно обновить кэш
await leaderboardManager.forceUpdate();
```

## Примеры использования

### Показать топ 10:

```javascript
const leaderboardManager = new LeaderboardManager(authManager);
const topPlayers = await leaderboardManager.getTopPlayers(10);
topPlayers.forEach((player, index) => {
    console.log(`${index + 1}. ${player.username}: ${player.bestScore} очков`);
});
```

### Получить ранг текущего пользователя:

```javascript
const userStats = await leaderboardManager.getUserStats(currentUser.uid);
console.log(`Ваш ранг: ${userStats.rank}`);
console.log(`Ваш лучший результат: ${userStats.bestScore}`);
```

### Поиск игрока:

```javascript
const results = await leaderboardManager.searchPlayers("John", 10);
results.forEach(player => {
    console.log(`${player.rank}. ${player.username}: ${player.bestScore}`);
});
```

## Структура папок

```
Tetris/
├── index.html                    # Страница входа
├── game.html                     # Игровая страница
├── leaderboard.html              # Таблица лидеров ✨ НОВОЕ
├── LEADERBOARD_SYSTEM.md         # Документация ✨ НОВОЕ
├── css/
│   ├── auth.css
│   ├── game.css
│   └── leaderboard.css           # Стили лидеров ✨ НОВОЕ
├── js/
│   ├── firebase.js               # Firebase конфигурация ✏️ ОБНОВЛЕНО
│   ├── auth.js                   # Аутентификация ✏️ ОБНОВЛЕНО
│   ├── game.js                   # Игровая логика
│   ├── game-manager.js           # Управление игрой ✏️ ОБНОВЛЕНО
│   ├── game-page.js              # Контроллер игры ✏️ ОБНОВЛЕНО
│   ├── leaderboard.js            # Менеджер лидеров ✨ НОВОЕ
│   ├── leaderboard-page.js       # Контроллер лидеров ✨ НОВОЕ
│   ├── utils.js
│   └── app.js
```

## Тестирование

### Тест 1: Таблица лидеров видна

1. Создайте 2-3 аккаунта
2. Сыграйте несколько игр с разными результатами
3. Откройте таблицу лидеров
4. Убедитесь, что игроки отсортированы по результатам

### Тест 2: Поиск работает

1. Откройте таблицу лидеров
2. Введите часть имени игрока в поиск
3. Убедитесь, что результаты отфильтрованы

### Тест 3: Фильтры работают

1. Нажмите "Топ 10" - должны показаться 10 лучших
2. Нажмите "Около меня" - должны показаться игроки рядом с вами
3. Нажмите "Все игроки" - должны показаться все

### Тест 4: Персональная статистика

1. Откройте таблицу лидеров
2. Проверьте, что ваш ранг совпадает с вашей позицией в таблице
3. Проверьте, что лучший результат совпадает с вашей записью

## Возможные проблемы и решения

### Q: Таблица лидеров не загружается
**A:** Проверьте:
1. Авторизованы ли вы?
2. Есть ли интернет соединение?
3. Открыты ли в браузере разработчика консоль (F12) - посмотрите ошибки

### Q: Рейтинг не обновляется
**A:** 
1. Нажмите кнопку "Обновить" 
2. Проверьте, что Firebase Security Rules позволяют читать данные

### Q: Поиск не работает
**A:**
1. Убедитесь, что вводите минимум 2 символа
2. Проверьте правильное написание имени

## Производительность

- Лучше всего работает с до 1000 активных пользователей
- Для большего количества рекомендуется использовать Firestore Indexes
- Кэширование снижает нагрузку на БД на 80%

## Лицензия

Тот же проект с расширением функционала таблицы лидеров.
