# Развертывание и Настройка Мультиплеера

## 🚀 Быстрый Старт

### Шаг 1: Проверить Firebase Конфигурацию
Убедитесь что в файле `js/firebase.js` правильно настроена конфигурация Firebase:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  databaseURL: "https://your-project.firebaseio.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### Шаг 2: Включить Realtime Database
1. Перейти на [Firebase Console](https://console.firebase.google.com)
2. Выбрать ваш проект
3. Перейти на "Realtime Database"
4. Нажать "Create Database"
5. Выбрать локацию
6. **ВАЖНО:** Установить режим на "Start in test mode" для разработки

### Шаг 3: Настроить Firebase Rules
В Firebase Console перейти на "Rules" вкладку и заменить на:

```json
{
  "rules": {
    "multiplayer_rooms": {
      "$roomCode": {
        ".read": true,
        ".write": "root.child('createdBy').val() === auth.uid || 
                   root.child('player1/id').val() === auth.uid || 
                   root.child('player2/id').val() === auth.uid",
        ".validate": "newData.hasChildren(['code', 'createdBy', 'player1'])",
        ".indexOn": ["createdAt", "status"]
      }
    }
  }
}
```

### Шаг 4: Проверить Структуру Файлов
```
Tetris/
├── index.html
├── menu.html              ← Новый
├── game.html
├── multiplayer.html       ← Новый
├── multiplayer-game.html  ← Новый
├── leaderboard.html
├── js/
│   ├── firebase.js
│   ├── auth.js
│   ├── game.js
│   ├── game-page.js      (обновлен)
│   ├── auth-page.js      (обновлен)
│   ├── menu-page.js      ← Новый
│   ├── multiplayer-page.js      ← Новый
│   ├── multiplayer-room.js      ← Новый
│   ├── multiplayer-game-manager.js ← Новый
│   ├── multiplayer-game-page.js    ← Новый
│   └── ...
├── css/
│   ├── menu.css          ← Новый
│   ├── multiplayer.css   ← Новый
│   ├── multiplayer-game.css ← Новый
│   ├── game.css
│   └── ...
└── ...
```

### Шаг 5: Обновить Начальную Страницу
Убедитесь что `index.html` перенаправляет на `menu.html` после входа.

**Текущее поведение после входа:**
- index.html → auth-page.js → menu.html (вместо game.html)

## 🔧 Конфигурация

### Параметры Синхронизации

В файле `js/multiplayer-game-manager.js` можно изменить интервал синхронизации:

```javascript
constructor(roomCode, playerId, game1, game2 = null) {
    // ...
    this.syncDelay = 100; // Синхронизация каждые 100ms
    // ...
}
```

Рекомендуемые значения:
- **50ms** - Очень частая синхронизация (больше нагрузка)
- **100ms** - Сбалансировано (по умолчанию)
- **200ms** - Менее частая (меньше нагрузка)

### Время Жизни Комнаты

В файле `js/multiplayer-room.js`:

```javascript
expiresAt: new Date(Date.now() + 30 * 60 * 1000).toISOString() // 30 минут
```

Измените число для изменения времени жизни комнаты.

## 📊 Мониторинг Производительности

### Firebase Usage

Проверить использование Firebase:
1. В Firebase Console → "Usage"
2. Смотреть количество чтений/записей
3. Оптимизировать если нужно

### Оптимизация

Если заметили высокое использование:

1. **Увеличить интервал синхронизации** (с 100ms на 200ms)
2. **Компрессировать данные** перед отправкой в Firebase
3. **Кэшировать** часто запрашиваемые данные
4. **Использовать batching** для нескольких обновлений

## 🐛 Отладка

### Включить логирование

Добавить в начало файлов:

```javascript
const DEBUG = true;

if (DEBUG) {
  console.log('Game Manager initialized');
  console.log('Room Code:', this.roomCode);
  console.log('Player ID:', this.playerId);
}
```

### Проверить Firebase Connection

В браузер консоли:

```javascript
// Проверить статус подключения
firebase.database().ref('.info/connected').on('value', (snap) => {
  if (snap.val() === true) {
    console.log('Firebase подключен');
  } else {
    console.log('Firebase отключен');
  }
});
```

### Просмотреть Realtime Database

1. Firebase Console → Realtime Database
2. Можно смотреть все комнаты в реальном времени
3. Видить структуру данных и изменения

## 🚨 Проблемы и Решения

### Проблема: "Комната не найдена"
**Решение:**
- Проверить что код комнаты правильный (8 символов)
- Проверить что создатель все еще в комнате
- Проверить Firebase подключение

### Проблема: "Соединение потеряно"
**Решение:**
- Проверить интернет соединение
- Перезагрузить страницу
- Проверить Firebase правила доступа

### Проблема: Данные не синхронизируются
**Решение:**
- Проверить что оба игрока в одной комнате
- Проверить что Firebase правила позволяют писать данные
- Проверить браузер консоль на ошибки
- Попробовать увеличить `syncDelay`

### Проблема: Высокое использование Firebase
**Решение:**
- Увеличить интервал синхронизации
- Установить лимиты на количество игр
- Автоматически удалять старые комнаты
- Использовать Firebase правила для ограничения

## 📱 Мобильная Оптимизация

### Для лучшего опыта на мобильных:

1. **Уменьшить размер игровой доски** для более узких экранов
2. **Вертикальная ориентация** игровых досок на мобильных
3. **Более крупные кнопки** для сенсорных устройств

Это уже реализовано в `css/multiplayer-game.css`:

```css
@media (max-width: 1200px) {
    .multiplayer-game-main {
        grid-template-columns: 1fr;
        gap: 20px;
    }
}
```

## 🔐 Безопасность в Production

Перед развертыванием в production:

1. **Изменить Firebase Rules** с "test mode" на более строгие правила
2. **Включить Email Verification** в Firebase Auth
3. **Установить Rate Limiting** на Firebase функции
4. **Использовать HTTPS** для всех соединений
5. **Скрыть Firebase конфиг** если возможно (используя environment variables)

### Production Firebase Rules

```json
{
  "rules": {
    "multiplayer_rooms": {
      "$roomCode": {
        ".read": "root.child('player1/id').val() === auth.uid || 
                   root.child('player2/id').val() === auth.uid",
        ".write": "auth.uid != null && 
                   (root.child('createdBy').val() === auth.uid || 
                    root.child('player2/id').val() === auth.uid)",
        ".validate": "newData.hasChildren(['code', 'createdBy', 'player1']) &&
                     $roomCode.matches(/^[A-Z0-9]{8}$/")"
      }
    }
  }
}
```

## 📦 Деплой

### На Firebase Hosting:

```bash
# 1. Установить Firebase CLI
npm install -g firebase-tools

# 2. Логинутся в Firebase
firebase login

# 3. Инициализировать проект
firebase init hosting

# 4. Дебилдить приложение (если нужно)
# (В нашем случае это просто HTML/CSS/JS)

# 5. Дебилдить
firebase deploy --only hosting
```

### На GitHub Pages:

1. Загрузить на GitHub
2. Включить GitHub Pages в настройках репозитория
3. Выбрать ветку для деплоя

## ✅ Чек-лист перед Запуском

- [ ] Firebase Realtime Database создана и включена
- [ ] Firebase Rules заданы правильно
- [ ] Все файлы загружены в правильные директории
- [ ] Firebase конфиг в `js/firebase.js` правильный
- [ ] Тестирование прошло успешно на локальной машине
- [ ] Браузер совместимость проверена
- [ ] Мобильная версия работает
- [ ] Обработка ошибок работает
- [ ] Документация обновлена
- [ ] Git коммит сделан

## 📞 Поддержка

Если возникают проблемы:

1. Проверить браузер консоль на ошибки
2. Проверить Firebase Console на использование
3. Проверить Firebase Rules на правильность
4. Смотреть документацию в `MULTIPLAYER_GUIDE.md`
5. Включить DEBUG логирование

---

**Готово!** Ваше приложение с мультиплеером готово к использованию! 🎉
